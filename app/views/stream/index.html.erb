<% content_for :head do %>
    <link href="<%= request.path+'.atom?'+request.query_string %>" rel="alternate" title="ATOM" type="application/atom+xml" />
<% end %>

<%= render :partial => 'photo', :collection => @photos %>

<%= will_paginate @photos %>
